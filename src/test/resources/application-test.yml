
spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2

  mail:
    host: 127.0.0.1
    port: 3025
    password: buildoptima
    username: managementstaffing09@gmail.com

aws:
  properties:
    s3:
      bucketName: buildoptima-test

config:
  aws:
    s3:
      region: us-east-1
      url: http://localhost:4566
      bucketName: ${aws.properties.s3.bucketName}
      accessKey: localstack
      secretKey: localstack

  uris:
    faq-category:
      baseUri: /faq/categories
      deleteByIdUri: /{id}
      updateUri: /{id}
      fetchUri: /fetch
      retrieveByIdUri: /{id}
      exportInCsvUri: /csv
    faq-question:
      baseUri: /faq/questions
      deleteByIdUri: /{id}
      updateUri: /{id}
      fetchUri: /fetch
      retrieveByIdUri: /{id}
      exportInCsvUri: /csv
    user:
      baseUri: /user
      fetchUri: /fetch
      retrieveByIdUri: /{id}
      deleteImageByIdUri: /{id}/image
      downloadImageUri: /{id}/
      uploadImageUri: /{id}/image
      restorePasswordUri: /auth/password/restore
      verifyPasswordUri: /auth/password/verify
      changePasswordUri: /password/change
      refreshTokenUri: /auth/refreshToken
      loginUri: /auth/login
      activationUri: /auth/activate
      registrationUri: /auth/registration
