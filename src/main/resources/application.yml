spring:
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/build_optima?currentSchema=build_optima

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
